out/
__pycache__/
.pytest_cache/
*.pyc

# Cached vendor documentation pages produced by `discover.py --crawl`.
fixtures/vendor_docs/

# Integrity Layer runtime state (per-customer history, baselines, registry)
state/*
!state/.gitkeep

# Integrity Layer per-run outputs
out/*
!out/.gitkeep

# Discovery runtime state
state/discovery_cache/
fixtures/vendor_schemas/*
!fixtures/vendor_schemas/.gitkeep
# NOTE: the bare `out/` on line 1 already blanket-ignores this directory;
#       the two lines below are informational only. Use `git add -f` for
#       .gitkeep markers under out/*/.
out/discovery_reports/*
!out/discovery_reports/.gitkeep
