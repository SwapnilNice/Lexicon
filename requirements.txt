lxml>=5.0
PyYAML>=6.0
pytest>=8.0
beautifulsoup4>=4.12
httpx>=0.27
pypdf>=6.0                # PDF fetch/extract for vendor docs delivered as PDFs
playwright>=1.40          # OPTIONAL — headless browser for JS-rendered doc sites.
                          # After install run once: `python -m playwright install chromium`
