[pytest]
# importlib mode gives each test file a unique module identity derived from
# its file path, so sibling namespace-package directories can each contain a
# file named test_models.py without a collision.
addopts = --import-mode=importlib
